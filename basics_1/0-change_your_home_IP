#!/usr/bin/env bash
# Configures /etc/hosts: localhost -> 127.0.0.2, facebook.com -> 8.8.8.8

sudo sed -i '/localhost/s/127.0.0.1/127.0.0.2/' /etc/hosts
if ! grep -q '^8.8.8.8[[:space:]]facebook.com' /etc/hosts; then
  echo '8.8.8.8 facebook.com' | sudo tee -a /etc/hosts > /dev/null
fi
